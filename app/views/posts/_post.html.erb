<div class="well" %>
	<h5><%= post.title %></h5>
	<hr/>
	<p><%= post.body%></p>
	<hr/>
	<p>Created by: <%= link_to post.creator.username, user_path(post.creator), class: "course-a" %> 
			at <%= post.created_at %>
	</p>
</div>